
<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteBase /tercerAnio/sanalfonso/codeigniter/

    # Elimina index.php de las URLs
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteRule ^(.*)$ index.php/$1 [L]
</IfModule>
